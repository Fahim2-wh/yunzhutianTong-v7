真实YOLO模型放置说明：

1. 工地安全检测模型：
   把训练好的模型重命名为 site_safety.pt
   放到本文件夹中。

2. 裂缝/结构病害检测模型：
   把训练好的模型重命名为 crack_detection.pt
   放到本文件夹中。

3. 重新启动网站后，首页会自动识别模型是否存在。

没有模型时，系统仍然可以使用“试点演示模式”完成：
上传照片 → 图像分析 → 生成隐患 → 整改闭环 → 导出报告。
